<html>
  <head>
    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
      google.charts.load('current', {'packages':['corechart']});
      google.charts.setOnLoadCallback(drawChart);

      function drawChart() {
        var data = google.visualization.arrayToDataTable([
          ['Zusatznutzen', 'Rabatte'],
          [ 4,      12.6],
          [ 6,      36.2],
          [ 2,     14],
          [ 3,      25],
          [ 2,      14.5],
          [ 1,    7]
        ]);

        var options = {
          title: 'Zusatznutzenkategorie und Rabat - Orphan Drugs',
          hAxis: {title: 'Zusatznutzen', minValue: 0, maxValue: 7},
          vAxis: {title: 'Rabatmenge', minValue: 0, maxValue: 80},
          legend: 'none'
        };

        var chart = new google.visualization.ScatterChart(document.getElementById('chart_div'));

        chart.draw(data, options);
      }
    </script>
  </head>
  <body>
    <div id="chart_div" style="width: 900px; height: 500px;"></div>
    <div>Hier die Zahlen werden dann durch die Zusatznutzenkategorien ausgetauscht und dienen nur als hinter liegender Code .Kategorie 7 dient nur als Abstand , damit Kategorie 6 nicht halbiert wird/abegschnitten wird .Kreise werden noch durch Rauten ausgetauscht für bessere Sichtbarkeit</div>
  </body>
</html>

